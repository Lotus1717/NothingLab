services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      SQLITE_PATH: /data/quota.db
    volumes:
      - quota_data:/data
    restart: unless-stopped

volumes:
  quota_data:
